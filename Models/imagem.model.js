class Imagem {
    constructor(id, referencia, titulo, data_criacao) {
        this.id = id;
        this.referencia = referencia;
        this.titulo = titulo;
        this.data_criacao = data_criacao;
    }
}

module.exports = Imagem;
